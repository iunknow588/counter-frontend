export declare function generateGoogleUrl({ nonce, clientId, redirectUri, scope, prompt, }: {
    nonce: string;
    clientId: string;
    redirectUri: string;
    scope?: string;
    prompt?: string;
}): string;
